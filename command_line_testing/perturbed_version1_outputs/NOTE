This folder was (and may still be, if wanted) used to store resulting heatmaps after the basic test file running version1_prototype_main with perturbations.

Since version1_prototype_main is an outdated terminal-based version of the first part of the project and does include all the features found in full interactive app.py, it has no use.

